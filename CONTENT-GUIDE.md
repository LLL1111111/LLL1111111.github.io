# 内容发布指南

网站由 `content/` 中的数据自动生成。不要直接编辑旧版根目录 HTML。

## 浏览器发布

1. 打开 https://app.pagescms.org，用 GitHub 登录并授权此仓库。
2. 选择 `lll1111111/lll1111111.github.io` 的 `main` 分支。
3. 在 Insights and materials 中创建或编辑内容，填写标题、固定英文 slug、原始日期、主题、格式和摘要。
4. 添加正文，上传 PDF / PPTX 和封面。PPTX 提供下载；需要在线预览时同时提供 PDF 版本。
5. 准备好后开启 Publish 并保存。GitHub Actions 会构建、检查链接并部署网站。

首次 GitHub 登录与仓库授权需要你本人完成；配置文件已提供，尚未代你验证第三方登录。

## 草稿与隐私

归档资料只导入了文件名和分类，未批量上传原始文件。草稿不进入公开网页、搜索、RSS 或 sitemap；但本仓库是公开仓库，草稿源文件仍可被 GitHub 访客看到。敏感内容请留在本地或私有仓库，不要提交到这里。

原始日期代表写作时间，网站发布时间另填。索引推断的旧资料日期需发布前核对。文章中的事实、引文和图片权属也应在发布前复核。

## 稳定网址

发布后不要修改 slug。修改标题、摘要、主题不会影响网址。旧 Blog、Essays、Research、CV 链接保留跳转，原研究页地址不变。

## Future Map 与项目

Future Map 管理项可修改当前研究重点、未决问题、观察信号和历史记录。新增证据时追加 history 记录，不覆盖过去判断。Projects 可更新问题、方法、成果链接和状态。

## 本地操作

```powershell
npm ci
npm run build
npm run check
node scripts/serve.mjs
```

本地预览为 http://127.0.0.1:4178。`dist/` 是部署产物，不包含草稿或编辑配置。

## X 内容

Notes 使用 X 官方时间线嵌入，并有失败提示。这不是本地同步或归档。要让短观点永久保留在网站，在 Insights 中创建 Note 内容；自动从 X 同步需要额外可用的数据接口和授权。

## 大文件

仓库附件应控制体积；接近 GitHub 单文件限制的 PPT 或视频应先压缩或使用独立文件存储。当前构建只接受仓库内的附件路径，并会检查文件是否存在。
